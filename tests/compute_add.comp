#version 440

layout(local_size_x = 64) in;

layout(std430, binding = 0) buffer layout0 { float arr_A[]; };
layout(std430, binding = 1) buffer layout1 { float arr_B[]; };
layout(std430, binding = 2) buffer layout2 { float arr_C[]; };

void main()
{
    uint idx = gl_GlobalInvocationID.x;

    arr_C[idx] = arr_A[idx] + arr_B[idx];
}
